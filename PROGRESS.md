# Стан робіт

Оновлено: 19 серпня 2026. Технічний опис проєкту — в `README.md`, робочі
інструкції — в `CLAUDE.md`. Тут лише те, що зроблено, що заблоковано і що далі.

## Зроблено

| Пункт                         | Стан                                                                                     |
| ----------------------------- | ---------------------------------------------------------------------------------------- |
| A. Публікація тестового поста | цикл container → publish → insights перевірений, замінений типізованим клієнтом          |
| B. Bash-обгортки              | не потрібні: `scripts/threads.ts` і `scripts/agent.ts` покривають ті самі операції       |
| C. Long-lived token           | обмін і продовження в клієнті, токен до 18 жовтня 2026                                   |
| D. «Офіс» агента              | `content/` з `queue/`, `published/`, `knowledge/`; формат описаний у `content/README.md` |
| E. `config.json`              | `agent.config.json`: слоти, денний ліміт, обмеження тексту, ключові слова                |
| F. Інструкція для агента      | `.claude/skills/threads-pr-manager/SKILL.md`                                             |
| H. Terms of Service           | окрема `docs/terms.html`, у ефірі                                                        |
| G. Джерело контенту           | генерація через Claude по voice-конфігу й контент-плану; лишається наповнити `knowledge/` |

Поверх плану додано: JSON API (`server/`), Vue-дашборд, моніторинг вхідних,
89 тестів на Vitest, CI на Node 22 і 24, Postgres у Docker для історії
генерацій і метрик, полиця чернеток.

## Заблоковано рівнем доступу застосунку

Лишився один пункт, не два.

- **Пошук за словом поза власним акаунтом.** `keyword_search` відповідає, але на
  базовому рівні доступу віддає лише пости `@calendarsync`. Монітор їх
  відсіює, тож зовнішнього сигналу зараз нема. Потрібен Advanced Access, а він
  вимагає підтвердженої компанії — портфоліо **Casy** (`1413420254096555`) зараз
  «Не підтверджено», і в ньому не заповнено ні юридичної назви, ні адреси, ні
  телефону, ні сайту. Це передумова, не наслідок: подавати App Review до
  верифікації нема сенсу. Далі — заявка зі скрінкастом user flow, 2–4 тижні.

**Згадки розблоковані 20 серпня 2026** і в App Review не впиралися ніколи.
`threads_manage_mentions` був у токені, але не був доданий до Use Case «Access
the Threads API» — у таблиці Permissions and features його рядок стояв з дією
`+ Add` замість статусу. API віддавав це як `code 10` subcode 4279067
(«insufficient app access level»), що читається як рівень доступу і ним не є.
Одне натискання `Add`, і той самий токен, без перевипуску, віддає
`{"data":[]}`. Урок на майбутнє: scope у `debug_token` не доводить, що дозвіл
увімкнений — звіряти з таблицею Use Case.

## Відкрите

### G. Матеріали для генерації — те, що лишилось від вузького місця

Механіка готова: `agent/generator.ts` пише через Claude, беручи голос із
`voice` в `agent.config.json`, теми з `content/plan.md` і матеріали з
`content/knowledge/`. І CLI (`agent.ts generate`), і дашборд працюють.

Порожня `knowledge/` не ламає генерацію, але робить її загальною: модель прямо
інструктована не вигадувати цифри й кейси, яких не бачила. Тож лишається:

- 5–10 реальних постів, краще з каналу з історією
- транскрипти демо і дзвінків з клієнтами — звідти беруться слова, якими
  клієнти самі описують свої проблеми
- опис продукту: що Casy робить, скільки коштує, чого свідомо не робить

`ANTHROPIC_API_KEY` у `.env` — без нього генерація не запуститься. На живому
акаунті ще не проганялася, лише на мок-відповідях у тестах.

### I. Tech Provider verification

Не потрібно, поки працюємо на власному акаунті. Знадобиться лише для доступу до
чужих Threads-акаунтів: окрема верифікація ~тиждень плюс App Review на кожен
дозвіл.

### Розгортання

Рішення: **на Pi їде тільки застосунок**, legal-сторінки лишаються на GitHub
Pages за адресою `rayofgoodness.github.io/threads-agent/`. Meta вимагає їх
доступними постійно, а домашній Pi вимикається — тоді privacy й data-deletion
віддавали б 502.

Дашборд буде на `https://threads.quarters.casa` через Cloudflare Tunnel —
піддомен уже наявного на малині тунелю, який поруч тримає `observer.`,
`grafana.` і `mcp.`. Встановлення — `sudo bash deploy/install.sh` на самій
малині, решта в `deploy/README.md`.

Перенесення legal на піддомен описане runbook'ом у `deploy/README.md`, але
відкладене — і потребує іншого імені, бо `threads.` тепер зайнятий дашбордом. Порядок там не довільний: GitHub вмикає 301 на власний домен
одразу, тож домен, заданий раніше за DNS-запис, кладе саме ті URL, що
зареєстровані в Meta. Перевірено на практиці 19 серпня.

### Чернетки

Згенерований пост має два виходи: `content/drafts/` — полиця без слоту, куди
`runDue` не заглядає, і `content/queue/` — розклад. Перехід між ними один
явний крок (`agent.ts schedule` або кнопка). Чернетку можна правити на місці,
видалити, і повернути з історії генерацій.

### База даних

Postgres у Docker на `127.0.0.1:55432` (не 5432, щоб не битися з локальним).
Тримає лише історію генерацій і метрики постів — контент лишається файлами в
git. Без `DATABASE_URL` усе працює так само, історія просто не пишеться.
Підняти: `npm run db:up && npm run db:migrate`.

### Дрібніше

- Черга не публікується сама — потрібен cron або launchd на `agent.ts run --yes`
- Метрики пишуться лише за викликом `POST /api/posts/:id/metrics` — періодичного
  збору ще немає, крива будується лише з того, що запитали руками
- Історія генерацій живе лише в Postgres: без `DATABASE_URL` вона не пишеться
  взагалі, і повернути старий варіант на полицю нема звідки
- `refresh_access_token` треба викликати до 18 жовтня, інакше повна авторизація
  заново
- Медіа-пости (IMAGE, VIDEO, CAROUSEL) реалізовані в клієнті, але на живому
  акаунті не перевірені — тільки TEXT

## Нотатки з процесу

Те, що коштувало часу і не очевидне з коду:

- **Токен беруть у User Token Generator** (Use Case → Access the Threads API →
  Settings), а не через OAuth-URL. Authorize віддає `error_code: 1`, а отриманий
  ним код несе дозволи на момент **первинної** авторизації акаунта — додані
  пізніше в токен не потрапляють, скільки не переавторизовуйся.
- `issued_at` у `debug_token` — час первинної авторизації, а не видачі токена.
  Однаковий `issued_at` не доводить, що токен той самий.
- **Threads App ID ≠ Meta App ID.** У `.env` перший (`1860340038261236`), у
  дашборді видно другий (`1794866701670684`).
- Redirect/Callback URLs — всередині Use Case, не в загальних App Settings.
- Видалення вимагає `threads_delete`, і відмова прилітає **після** того, як пост
  став публічним.
- `/me/threads` відстає від записів на кілька секунд.
- `/me/replies` — це відповіді, **написані** акаунтом, а не адресовані йому.
  Вхідні читаються обходом власних постів.
- Node type stripping не компілює: parameter properties, `enum` і `namespace`
  падають при запуску, хоча `tsc` їх пропускає.
- `dev.meta.ai` — окремий geo-заблокований продукт, не плутати з
  `developers.facebook.com`.
- **Ніколи не вставляти access token чи app secret у чат або git** — лише `.env`,
  який у `.gitignore`.
